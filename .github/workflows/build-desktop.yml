name: Build Desktop Agent

on:
  push:
    branches:
      - main
    paths:
      - 'desktop-agent/**'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: windows-latest
            platform: win
          - os: macos-latest
            platform: mac

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: desktop-agent
        run: npm install

      - name: Download Kubo binary
        working-directory: desktop-agent
        shell: bash
        run: |
          if [ "${{ runner.os }}" == "Windows" ]; then
            node scripts/download-kubo.js win32-x64
          elif [ "${{ runner.os }}" == "macOS" ]; then
            node scripts/download-kubo.js darwin-x64
          else
            node scripts/download-kubo.js linux-x64
          fi

      - name: Build application
        working-directory: desktop-agent
        run: npm run build

      - name: Package for ${{ matrix.platform }}
        working-directory: desktop-agent
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USE_HARD_LINKS: false
        run: npm run package:${{ matrix.platform }}

      - name: Create archive (Linux/Mac)
        if: runner.os != 'Windows'
        working-directory: desktop-agent
        run: |
          cd build
          # Linux AppImage - already a single file, just list it
          if ls *.AppImage 1> /dev/null 2>&1; then
            echo "AppImage found:"
            ls -la *.AppImage
          fi
          if [ -d "mac" ]; then
            tar -czvf spk-desktop-mac.tar.gz mac/
          fi
          if [ -d "mac-arm64" ]; then
            tar -czvf spk-desktop-mac-arm64.tar.gz mac-arm64/
          fi
          ls -la

      - name: Create archive (Windows)
        if: runner.os == 'Windows'
        working-directory: desktop-agent
        shell: pwsh
        run: |
          cd build
          if (Test-Path "win-unpacked") {
            Compress-Archive -Path "win-unpacked" -DestinationPath "spk-desktop-win.zip"
          }
          Get-ChildItem

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: spk-desktop-${{ matrix.platform }}
          path: |
            desktop-agent/build/*.tar.gz
            desktop-agent/build/*.zip
            desktop-agent/build/*.AppImage
          if-no-files-found: warn

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release-files
          find artifacts -name "*.tar.gz" -exec cp {} release-files/ \;
          find artifacts -name "*.zip" -exec cp {} release-files/ \;
          find artifacts -name "*.AppImage" -exec cp {} release-files/ \;
          echo "Release files:"
          ls -la release-files/

      - name: Delete existing latest release
        run: |
          gh release delete latest --yes || true
          git push origin :refs/tags/latest || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Latest Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest Desktop Build"
          body: |
            ## SPK Desktop Agent - Latest Build
            
            **Auto-updated on every push to main.**
            
            Download for your platform:
            - **Windows**: `spk-desktop-win.zip` - Extract and run
            - **macOS**: `spk-desktop-mac.tar.gz` - Extract and run
            - **Linux**: `.AppImage` file - Make executable with `chmod +x` and run
            
            Built from commit: ${{ github.sha }}
          files: release-files/*
          draft: false
          prerelease: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
